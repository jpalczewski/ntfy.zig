[project]
name = "ntfy-zig-stress"
version = "0.1.0"
description = "Locust stress test + fake ntfy endpoint for ntfy.zig, in one environment"
requires-python = ">=3.14"
dependencies = [
    "locust>=2.32",
]

[tool.uv]
package = false
